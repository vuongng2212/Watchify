server.port=8888
spring.application.name=backend
# Active Profile
spring.profiles.active=dev

springdoc.swagger-ui.path=/swagger-ui.html
springdoc.api-docs.path=/v3/api-docs
springdoc.api-docs.enabled=true
springdoc.swagger-ui.enabled=true
springdoc.spec.path=classpath:/openapi.yaml

# Database Configuration
spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:watchify}
spring.datasource.username=${DB_USER:watchify}
spring.datasource.password=${DB_PASSWORD:1111}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.hibernate.ddl-auto=create
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# JWT Configuration
jwt.secret=3cfa76ef14937c1c0ea519f8fc057a80fde3c1f4e8f6b7a3c8d4e2f1a9b5c6d7e8f9a0b1c2d3e4f5g6h7i8j9k0l1m2n3o4p5
jwt.expiration=86400000
jwt.refresh.expiration=604800000
# 86400000 ms = 24 hours (access token)
# 604800000 ms = 7 days (refresh token)

# MoMo Payment Gateway Configuration
momo.partner-code=MOMOBKUN20180529
momo.access-key=klm05TvNBzhg7h7j
momo.secret-key=at67qH6mk8w5Y1nAyMoYKMWACiEi2bsa
momo.api-endpoint=https://test-payment.momo.vn/v2/gateway/api/create
momo.return-url=http://localhost:8888/api/v1/payments/return
momo.ipn-url=http://localhost:8888/api/v1/payments/ipn/momo

# Gemini AI Configuration
gemini.api-key=${GEMINI_API_KEY:}
gemini.model=gemini-1.5-flash
gemini.api-url=https://generativelanguage.googleapis.com/v1beta/models
gemini.system-prompt=You are Watchify's AI assistant. Answer in Vietnamese, be concise, and always cite product names, prices, and slugs when giving recommendations.

# ============================================
# EMAIL CONFIGURATION
# ============================================
# SMTP Configuration (Gmail)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:kudoyaung@gmail.com}
spring.mail.password=${MAIL_PASSWORD:rgbn apcs qvxf qlki}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# Email sender information
app.email.from=${MAIL_FROM:noreply@watchify.com}
app.email.name=Watchify E-commerce
app.email.support=support@watchify.com

# Frontend URLs for email links
app.frontend.url=${FRONTEND_URL:http://localhost:3001}
app.frontend.verify-email=/verify-email
app.frontend.reset-password=/reset-password
app.frontend.order-history=/history
